<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- DialogLoading theme. -->
    <style name="DialogLoading" parent="android:style/Theme.Dialog">

        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <!-- 分割线 -->
    <style name="splitLineStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dimens1dp</item>
        <item name="android:background">@color/colorEEEEEE</item>
    </style>

    <style name="SplashTheme" parent="AppBaseTheme">
        <item name="android:windowBackground">@drawable/layer_list_splash</item>
        <!-- 设置activity切换动画 -->
        <item name="android:windowAnimationStyle">@style/activityAnimation</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- animation 样式 -->
    <style name="activityAnimation" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/set_slide_right_in</item>
        <item name="android:activityOpenExitAnimation">@anim/set_slide_left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/set_slide_left_in</item>
        <item name="android:activityCloseExitAnimation">@anim/set_slide_right_out</item>
    </style>
    <!-- Button样式1 左右有间距 -->
    <style name="CommonBtn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dimens48dp</item>
        <item name="android:background">@drawable/btn_selector_red_gray</item>
        <item name="android:textColor">@drawable/text_selector_white_gray</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources>
